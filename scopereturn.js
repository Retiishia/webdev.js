const firstName = "Farhan";
const lastName = "Aziz";

function getFullName(firstName, lastName) {
    const fullName = firstName + " " + lastName;

    if(1 === 1) {
        console.log(fullName);
    }
    return fullName;
};
getFullName(firstName, lastName);



// CHALENNGE
function getNumber (x, y) {
  return x * y;
}

const result = getNumber(5, 10);
console.log(result);